# v1.3.1:
- Hide seldom used buttons
- Fix title of add group window
- Add option to main menu to sort contacts by name
- Add separate entries for groups in contact name
- Delete old group when renaming it
- Make groups unclickable in the contacts edit screen
- Change ordering of contact details
- Add border to groups wheel

# v1.3.0:
- Add version to exported vcf file
- Add birthday notifications, should display a banner inside the app.
- Add list of all birthdays

# v1.2.0:
- remove hidden options from groups screen
- Add goups icon next to contact which has already been added to a group
- Sort contacts by mod date

# v1.1.0
- Increase icon sizes

# Earlier versions:
- put groups button on top (remove it from inside '...' list)
- Remove floating button
- verify the cursor commit (removed, does not actually delete groups)
- PUT ALL LICENCES BELOW IN APP
- Remove call log
